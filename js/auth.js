alert("auth.js loaded");

document.addEventListener("DOMContentLoaded", () => {

    alert("DOM Loaded");

    const form = document.getElementById("registerForm");

    alert(form ? "Form Found" : "Form NOT Found");

});
